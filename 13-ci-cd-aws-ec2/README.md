# CI/CD Deployment to AWS EC2

This module demonstrates deploying a Dockerized application to an AWS EC2 instance using GitHub Actions.

The pipeline uses SSH-based automation to deploy containers only after
all CI checks succeed, representing a controlled and production-aligned CD workflow.